import {useEffect, useState} from 'react';
const useScanValue = (filter, scanStart) => {
  const [listOfActivos, setlistOfActivos] = useState([]);

  useEffect(() => {
    const scan = () => {};

    handleInserStyle(color, backgroundColor, maxHeight, minHeight);
    /*   return () => {
            removeStyle();
        } */
    // eslint-disable-next-line
  }, [color, backgroundColor, maxHeight, minHeight]);

  return listOfActivos;
};
export default useScanValue;
